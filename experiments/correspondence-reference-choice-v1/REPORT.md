# Correspondence reference choice: preparation stopped

**Disposition: INVALID construction. Neither method was evaluated on the musical panel, and no method/task choice was established.** This is a complete checkpoint of the authorized attempt, not a negative matching result. PR #18's frozen Result C remains unchanged.

The PM authorized one development-only comparison in [comment 5842993369](https://github.com/trinitron88/earworm/pull/2#issuecomment-5842993369). Preparation implemented the unchanged historical clean readout (D), the global pitch/time reference (G), its separately isolated exact-event diagnostic, and a fixed eight-family generator. No audio was rendered or extracted. MERT was not loaded, changed, trained, or downloaded.

## What completed

- The qualification ledger contains **29 charged non-musical calls: 28 passed and one retained fixture failure**. Twenty-three named mathematical checks and five isolated-process checks ultimately passed. The failed lower-scale fixture compressed a three-second return below the fixed two-second query and consequently included preceding material. Before musical construction, that fixture was corrected to a longer non-musical source; neither matcher changed. Both attempts remain recorded.
- The isolated-process checks exercised full-history D/G/exact, recent-only G, and source-removal exact. They checked destructive record removal and output-receipt timestamps. Children installed a Python filesystem/network API guard; this was not an adversarial security test. These are software checks, not the musical delay experiment.
- One construction attempt saved **35 of 40 planned musical sessions**: all five conditions for the four Bach excerpts and the first three new motifs. The fourth motif's first condition stopped construction. All source choices and seeds remain in the generation manifest.
- The saved-input validator checks the **105 archived input files and 14,010 observation rows** against their hashes, exact events, occupancy, one-second availability and excerpt provenance. It does not call a matcher or regenerate material.

## Exact stop and its limits

`materials.py::_session` stopped on `novel-20260926-04--unchanged` with:

> Unintended complete source-equivalent 12-event occurrence in history

The guard scans every twelve-event historical window for equality of successive signed pitch intervals. It expects exactly the source window at index 2, after a two-event lead-in. The exception therefore indicates an additional match under that **pitch-only construction check**. The check ignores timing. Its wording must not be interpreted as proof of the same complete pitch/time transformation, perceptual identity, or a scientifically certified ambiguity.

The constructor saved the completed sessions and exception, but **did not save the failing session's generated source/history arrays or offending window indices**. Consequently the precise additional window cannot be independently inspected from this snapshot. The seed and generator are retained, but no forensic regeneration, replacement seed, generator repair or continuation was performed. This evidence-capture limitation is part of the invalid outcome.

The PM explicitly required construction failures to be checkpointed without replacement shopping. The attempt stopped before a preregistration receipt or any fresh musical candidate exposure. The separate eight-case diagnostic suite is specified in code but was not generated or run. There is **no ambiguity certificate**, no Result A/B/C classification for this stage, no rate-panel score and no method selection.

## What this does and does not change

Software qualification supports inspecting the two implementations; it does not qualify either as the development correspondence reference. The intended comparison remains unresolved. The source/pitch/timing/refusal distinctions in the September 25 retrospective are still historical evidence, not replicated findings here.

The new reference implements a global positive time scale and pitch translation inspired by Lemström's S1 formulation. Its sampled transition brackets, consecutive-run verifier, silence treatment, uncertainty reporting and abstention are Earworm adaptations. Exact-event input is richer than sampled rows and remains separately isolated. See [the method specification](GLOBAL_METHOD.md). Neither implementation is an information ceiling.

## Resources and disposition

Actual: 29 qualification calls, **0/264 planned musical calls**, **0/24 diagnostic calls**, one input-construction attempt, no reruns or replacements, and **$0**. See [resources.json](resources.json) for measured calls, artifact bytes, conservative preparation accounting and publication reserve. Existing checkout/history storage is reported separately from newly produced stage artifacts. No timing or raw-audio-eviction claim is made from this clean-observation preparation.

**Next action:** the existing reviewer should assess this exact invalid checkpoint; the PM then owns one explicit disposition addressing the pitch-only construction guard and missing failure witness. Resuming scientific work requires a bounded PM instruction specifying permitted evidence recovery or construction changes and treatment of the already-generated development material. The Actuator owns any authorized implementation and checkpoint. This publication does not authorize a repair or rerun.

## Evidence paths

- [Assignment](assignment.md), [implementation/preparation protocol](PROTOCOL.md)
- [Historical code hashes](baseline-hashes.json), [global method](GLOBAL_METHOD.md), [material rules](MATERIALS.md)
- [Non-musical qualification ledger](qualification/ledger.json)
- [Original construction failure and completed-input hashes](inputs/panel/construction-failure.json)
- [Fixed generator, source ranges and seeds](inputs/panel/generation-manifest.json)
- [Independent saved-input validation](saved-input-validation.json)
- [Snapshot manifest](freeze.json), [publication provenance](publication.json)

No files from historical experiments are changed. Root `PROJECT_STATE.md` records this disposition while retaining the finite capability checklist and first-release acceptance plan.
