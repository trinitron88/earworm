EARWORM_ASSIGNMENT_ID=earworm-correspondence-reference-choice-v1
EARWORM_INSTRUCTION_VERSION=1
EARWORM_BASE_SHA=07651558671fdaf57b5685284079cea021b0ea88
EARWORM_PM_DISPOSITION=development-method-or-task-choice
EARWORM_RECONCILES_COMMENT=5779286158
EARWORM_RETROSPECTIVE_COMMENT=5842902982

## PM decision and authorization to the existing Actuator

Brian has communicated the [September 25 retrospective](https://github.com/trinitron88/earworm/pull/2#issuecomment-5842902982) to the PM and Actuator and asked the PM to choose the next bounded disposition. Under [continuous in-plan delegation](https://github.com/trinitron88/earworm/pull/2#issuecomment-5747969943), I authorize the ONE development-only comparison below. This is the concrete assignment the existing Actuator was waiting for; proceed through the existing local continuation, without another routine approval from Brian.

**Reconciliation:** my closing rationale in comment 5779286158 applied the decision filter too narrowly. A successor can change the next method or task decision without changing past route adoption. A worker stop closes that assignment, not PM planning. After review, the PM must record the scientific change and either choose one bounded decision-qualified disposition or name a concrete blocker, owner and resume condition. This clarification does not authorize an automatic repair chain.

PR #18 remains closed and immutable: publication `3682acd09d927ca31df8429529de5119f5380ec3`, execution `db33138b7eb6336fe99b7f9260558fc9a8a6b6b0`, merge `07651558671fdaf57b5685284079cea021b0ea88`, [review](https://github.com/trinitron88/earworm/pull/18#pullrequestreview-5280187978). Its valid frozen Result C and non-adoption stand. This new instruction supersedes only the absence of a next assignment in the old stop receipt.

The retrospective's saved-record findings matter separately: standard audio identified 12/16 transposed and 12/16 stretched returns after 32 seconds and raw-buffer eviction; clean input recovered all 48 positive sources and all 48 exact shifts, with every clean joint failure in time-scale estimation. Accepted substitutes after source removal were standard 29/48, MERT 15/48, clean 21/48. Source dependence therefore does not establish absence rejection. These are historical findings, not new replications. Preserve causal prediction, explicit memory, evidence separation and simple retrieval controls. Keep the handcrafted continuous-front sequence closed.

## Decision before assignment

**Which finish-line capability does this assignment establish or unblock?** A usable correspondence diagnostic for once-heard transformed recurrence: identifying an earlier occurrence, estimating its pitch/time change, and refusing unsupported identities under a short query. It informs the next design decision toward the Bach/isolated-voice milestone; it is not audio-route adoption or the completed long-term-memory capability.

**Decision:** retain the current clean correspondence readout, replace it with one borrowed global pitch/time reference for development diagnostics, or revise the future query/identity contract when a concrete observational ambiguity is demonstrated.

- **Result A →** at least one equal-observation method meets the predeclared development requirements: select it as the development diagnostic reference. If both pass, retain the current simpler baseline. If only the borrowed reference passes, select it and record which timing/refusal failures it resolves on the same observations. The actionable output is a fixed interface/method for the next protocol, not a held-out success claim.
- **Result B →** neither passes and an independently checkable ambiguity witness shows that the permitted observations support incompatible stipulated identities: withhold method qualification and select a revision of the future query/identity contract for that witnessed ambiguity. Specify the necessary distinction (e.g. fragment-equivalence/abstention versus an identifiable whole occurrence); do not silently change labels in this study or launch a longer-query run.
- **Result C →** neither passes without such a witness: withhold qualification of these implementations, retain the current baseline for historical comparability, and classify unresolved failure by identity, shift, scale or refusal. If only the richer-score diagnostic succeeds, identify the observation/adapter difference explicitly; that is not evidence of acoustic encoder failure. Two failed methods do not prove impossibility.
- **INVALID/resource-blocked →** preserve the partial evidence and exact failure, owner and resume condition; no scientific negative or substitute candidate.

**Stop condition:** one frozen development panel, one checkable method/task disposition, one complete successor publication, then awaiting-review and stop. No automatic acoustic follow-up, capacity study, local repair, refit or confirmation is authorized. At review the PM must make the next deliberate choice; an acoustic comparison and a concrete retention-policy comparison remain competing options. Perfect transformation estimation is not a universal prerequisite for a narrower capacity question.

I choose correspondence now because the saved timing failure and accepted substitutes give a specific same-observation method/task distinction. This is more direct than adding replacement pressure before specifying what a refused or competing source means. A future capacity study must choose between actual retention policies against an unpressured baseline, not merely demonstrate predictable FIFO deletion.

## Two fixed candidates; equal observations first

**Candidate D — historical readout:** the PR #18 clean route from `matching.py` and `listener.py::predict(...,"ceiling")`, unchanged, including query selection, -5…+5 shift search, unweighted skip-step recurrence, single-best-path duration rejection, span-ratio estimate and clean acceptance threshold 0. Reuse/hash frozen code; do not repair it.

**Candidate G — borrowed global-transform reference:** use the time-scaled-translation model of Lemström (2010), **S1 / Section 3.1**, matching a query point pattern under one common positive time scale and pitch translation. Preserve explicit correspondence and transformation parameters rather than deriving scale from a DTW endpoint span. An exhaustive implementation is acceptable at this small size; an optimization must preserve its answers on qualification fixtures.
Primary specification: [institutional full text](https://helda.helsinki.fi/server/api/core/bitstreams/3ee07ff3-78ba-44a6-81d0-9c2b8ab7dbaf/content), [publication/DOI](https://doi.org/10.1007/978-3-642-12476-1_12).

The following **Earworm observation adapter and verifier are declared adaptations, not claims that the paper specifies or validates them**:

- D and G receive identical absolute-pitch/silence rows at 100 ms centers, occupancy cues, timestamps and completed-unit availability. G may derive maximal constant-pitch runs and observed transitions only from those rows. Retain the original rows losslessly. Do not infer hidden repeated-note onsets or receive score boundaries.
- Treat observed transition times as bracketed by their adjacent sample centers; do not label quantized boundaries as exact. Freeze the treatment of clipped query edges, silence, missing samples and adjacent repeated pitches. Enumerate one global affine time map and integer pitch shift in the same -5…+5 and [0.65,1.5] ranges. Handle sampling uncertainty using those fixed brackets, without fitted timing tolerances. Document the precise mapping from S1 to this interval-aware adapter, including any inability to represent a case.
- Verify every available query row/run against each candidate's mapped support, with explicit coverage and contradictions; no uncharged skipping of decisive evidence or matching across a removal hole. Candidate search and verification must not use a true source interval. Return all competing admissible occurrence regions, estimated parameters and feasible parameter ranges. Commit the deterministic ranking/tie/abstention rule before exposure; do not resolve genuinely distinct tied identities by an arbitrary earliest-source claim. Parameters too weakly constrained remain uncertain.
- No musical-data threshold grid, method sweep, query-length sweep or post-result change. G's verification rule is full consistency with the observed evidence under the declared sampling model; the implementation must disclose where that model leaves uncertainty. Its behavior is the tested candidate, not a theoretical information upper bound.

Use a separately labeled **G-exact-score diagnostic** on the same fixed time support and histories, where source generation supplies exact note times/pitches. This is the same transform model with richer observations, not a third competing matcher and not a like-input win. It cannot supply events, candidates, timing parameters or labels to D/G. Do not call either clean reference an information ceiling. No acoustic extraction, rendering, MERT inference/training/download, new encoder, memory controller or front-end repair is part of this stage.

## Finite development material and independent labels

Use **eight fresh development phrase families**:
- Four new, nonoverlapping 12-event excerpts, one each from already acquired development Bach works BWV 773, 775, 778 and 781. Use note indices 24–35 (zero-based) of the same isolated track under the saved provenance. These are new excerpts of previously exposed development works, not unseen-work validation. If an excerpt is malformed/unavailable or overlaps an earlier saved phrase, checkpoint it without replacement shopping.
- Four newly constructed motifs with new recorded seeds and no reuse of historical phrase signatures. Preserve their unfamiliar-by-construction status without claims about human or pretrained familiarity.

Keep PR #18's evaluation works, motifs, PCM, records and scores out of candidate development and reruns. No additional musical download is required or authorized here.

Each family has five fixed clean sessions: unchanged return, one transposition, one uniform stretch, a matched pitch-inventory/rhythm foil, and a near foil containing a decisive observed difference that a skip path could ignore. Preserve a three-second source, 32 seconds of intervening figures, the fixed last-two-second query rule and separate transformation conditions; balance shifts -5,-2,+2,+5 and scales 0.8/1.25 within the two material strata. Predeclare all generators, seeds, interference and foil rules independently of candidate scores. A construction failure is reported, not selected away.

For each of the three positive sessions, score full history, recent-only and source-record removal; score the two foils with full history. That is **88 readouts per candidate/diagnostic**, 264 total including G-exact-score. Remove every source-dependent record/cache/index and recompute derived state, using evaluator-only labels for removal. One-second arrivals, four-second recent store, at most 60 retained units and **8 MiB total predictor-accessible persistent storage per isolated method** remain explicit. Count derived indexes/caches, recurrent state and buffers; record scratch/RSS separately. No archive access, source pinning, hidden score access or bridge across removed units. This is clean-observation simulation, not a new claim of measured audio latency or raw-audio eviction; count actual recent-observation eviction and report zero persistent replacement pressure.

In addition, freeze **eight small diagnostic cases**, separate from the rate panel: four skip-sensitive cases and four short-fragment collision cases, including a paired-world construction with identical permitted observations but incompatible whole-source labels. Run the two methods and richer diagnostic at most once each (24 readouts). Distinguish a deliberately constructed limitation from an ambiguity actually witnessed in the eight musical families. An ambiguity certificate must show the available observation hashes/support and competing label/correspondence explanations; poor scores or a method's failure are not certificates. Do not mix impossible provenance distinctions into ordinary success rates or hide their outcomes.

## Frozen criteria and bounded execution

Before exposing the fresh musical panel, qualify code on **at most 32 tiny non-musical deterministic probes**, including known shift/scale signs, sampling brackets, exact-score S1 examples, no-match, ties, removal holes, and all-evidence coverage. Fix correctness problems only before panel exposure; retain probe outcomes and charge them. No qualified implementation within that allowance means checkpoint, not a different reference.

Publish a preregistration receipt on PR #2 before the panel: exact execution commit; protocol, candidate, verifier, generator, input/provenance, split, scoring, library and configuration hashes; source ranges/seeds; fixture ledger; frozen parameters; estimated resource reserve. All this stage's musical material is development; there is **no confirmatory split or held-out run in this assignment**. Any later confirmation requires a separate PM decision and fresh families. Freeze details left to implementation before exposure, consistent with this instruction, without asking Brian for routine choices.

For the eight-family panel, require separately:
- Accepted correct occurrence identity in at least 7/8 cases in **each** unchanged/transposed/stretched cell.
- Accepted correct source **and** exact pitch offset in at least 7/8 in each positive cell, including zero shift; report conditional estimation accuracy as well, never hiding rejection in the denominator.
- Accepted correct source **and** time scale within 10% relative error in at least 7/8 in each positive cell, including unchanged tempo. Report errors/uncertainty separately from identity and pitch.
- At most 1/16 accepted foils, and at most 2/24 accepted substitutes in source-removal, separately; recent-only at most 2/24 accepted returns. Correct source identity after removal is an integrity/label failure, not a success.
- Evidence preservation, access/removal accounting and all hard budgets pass. Report both material strata, every family's outcome, joint success, localization and skipped/verified coverage separately; no population-level qualification claim from eight development families.

Apply the predeclared rates to the whole fixed panel; no outcome-based exclusion of difficult/ambiguous families. The separate collision suite diagnoses the contract and is never used to retune the rate panel. Exact-score success cannot qualify the equal-observation candidate. No single combined score may conceal failed refusal or scale estimation.

Hard caps: **400 total method readouts/probes**, **7,200 aggregate local compute seconds including preparation/validation/publication reserve**, **512 MiB new artifacts**, **$0**. Planned 264+24+32=320 leaves 80 for independently counted verification calls, not new candidate trials. No resource-limit increase. Avoid regenerating unchanged inputs during validation; use saved outputs/hashes. If a usage limit interrupts work, checkpoint exact completed work, actual limit and known retry/reset information, rest, and resume only unexecuted authorized work when available; never busy retry, replay exposed panel runs or buy/reset credits.

## Publication, review and ownership

Start an isolated package `experiments/correspondence-reference-choice-v1/` from exact current main `07651558671fdaf57b5685284079cea021b0ea88`. Preserve all historical scientific files/models/results byte-for-byte, including PR #18 and its Result C. Update root `PROJECT_STATE.md` only in the ordinary complete publication: correct the stale awaiting-review wording, distinguish the clean reference from an upper bound, record separate refusal/timing/identity findings, and retain the finite capability checklist and first-release acceptance plan. No documentation-only PR.

Before starting, the existing Actuator must reconcile this comment ID/version/base and the latest PR #2 receipts with its durable processed-instruction state, acquire the existing no-overlap lock, and reuse matching work if present. Live inspection at instruction preparation found no open successor PR and main unchanged from PR #18. Do not duplicate, interrupt, restart or dispatch a second worker; keep the existing 30-minute continuation and persistent exact-head reviewer unchanged.

Deliver the concise branch decision, code/specification attribution and adaptations, frozen manifest, all saved inputs/outputs including negative/invalid cases, independently checkable arithmetic, ambiguity certificates or their absence, full resource/access/preservation ledger, and execution/publication provenance. An implementation/software failure, failure of a scientific criterion and strict localization failure must remain distinct.

Publish one complete successor targeting main: draft → apply `earworm-review-ready` → ready (or reuse a matching active successor with the label present). Link exact head, execution SHA, output paths and selected predeclared branch on PR #2; then checkpoint `awaiting-review` and stop. Existing review validates saved outputs and source hashes, reviews the exact head, and merges a sound positive/negative/inconclusive snapshot subject to normal gates. Do not push this work only to merged PR #18. The PM owns the post-review disposition; the Actuator owns implementation/checkpoints. Missing source/specification/access or resource evidence must name its owner and precise resume condition, not initiate another repair chain.
