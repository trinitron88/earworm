# The source was lost—and keeping it was not sufficient

**Result C: withhold the retention-policy decision.** The unpressured clean-observation reference failed the frozen14/16 recognition/transformation gate in every positive cell. No retention policy is selected. The valid saved comparison separates measured source eviction from a second, independent correspondence failure when the source survives.

This is exact note/event evidence with supplied occurrence segmentation, not an acoustic listener, a perceptual-identity test, or an information upper bound. The frozen G-exact implementation and all earlier scientific artifacts are unchanged.

## Held-out results

Sixteen fresh families: eight nonoverlapping Bach excerpts and eight newly generated motifs. Columns show jointly correct accepted occurrence, exact pitch shift and time scale within10%; identity and both separate transformation counts happen to agree here.

| Policy | Unchanged | Transposed | Stretched | Accepted foils | Accepted after source removal | Target retained in48 positive cells |
|---|---:|---:|---:|---:|---:|---:|
| FIFO |0/16|0/16|0/16|0/16|0/48|0/48|
| Uniform reservoir |0/16|0/16|0/16|0/16|0/48|0/48|
| Streaming diversity |0/16|0/16|0/16|0/16|0/48|0/48|
| Unpressured reference |8/16|8/16|6/16|0/16|0/48|48/48|
| Present-only |0/16|0/16|0/16|0/16|0/48|0/48|

The unpressured reference scores4/8,4/8,3/8 within **each** material stratum. Its26misses all say `no full-consistent occurrence`: none is an abstention caused by competing candidates. Independent full-event pitch/time witnesses remain within the declared arithmetic tolerance for these sources, and retained events pass reconstruction checks. The source exists in memory; frozen G nevertheless returns no candidate. This study does not isolate why, and no counterfactual matcher run or repair was performed. It would be inaccurate to attribute these misses to forgotten evidence or claim that their origin was proven ambiguous.

All22accepted unpressured positives have correct pitch and scale. The detailed report preserves candidate sets, their size, abstention, strict source identity, continuous localization errors and separate transform metrics in `score-evaluation.json`. Construction equivalence does not establish human musical identity.

## What actually disappeared

The source occupies0–3seconds; the query begins at516seconds, after128fixed interfering occurrences. FIFO evicts it at35seconds, reservoir at39seconds, and diversity between39and119seconds across families. All three have lost it before any query arrives. These losses are directly witnessed by emitted replacement ledgers, not inferred from failed matching. `retention-losses.json` lists all144bounded-policy positive cases.

Every pressured evaluation readout satisfies the eight-record capacity and at least12 actual replacements: FIFO122, reservoir27, diversity minimum44. Incoming rejections are reported separately. The present-only four-second ring has actually evicted the source; these are observation-buffer evictions, not a claim about raw-audio processing.

One common fixed reservoir seed was used. Its source loss repeats across families because arrival counts and source position match. Zero recall here is not an estimate of the policy's expectation across random seeds or random query positions. FIFO's first-arrived target loss is an expected consequence of the declared stress test. Diversity's loss does not establish that all possible selective retention methods fail.

The zero false-acceptance counts do not make the bounded methods adequate: they also reject every returning target. The assigned branch precedence uses the unpressured failure first, so this is ResultC, not ResultB or a clean policy ranking.

## Qualification, exposure and integrity

Fifteen predeclared tiny fixtures passed. Development and calibration each ran140 readouts. The unpressured joint counts were2/4,2/4,2/4 in development and2/4,2/4,1/4 in calibration. These known reference failures were explicitly disclosed in preregistration. The singleton descriptor weight1 and original unique-window abstention setting were never fitted or changed. All560 held-out readouts then ran once under the PM's fixed assignment; no exposed family was replaced and no setting was retuned.

Bach excerpts are from pinned, already-local public-domain source files. They do not overlap earlier saved event supports and are not full pitch/time equivalents of the saved historical phrases checked. The Bach works themselves were used previously; this is fresh-excerpt evidence, not unseen-work transfer. Works are separated across this study's splits and historical evaluation works stay evaluation. The fixed chronological monophony rule records33 rejected windows in BWV787 before selecting69–80; all other excerpts use36–47. These structural checks did not consult matcher output.

`global_matcher.py` is byte-identical to PR20. Its query remains the last two seconds (usually1.9seconds between sample centers); storing a complete12-event occurrence does not make the matching comparison whole-phrase. Supplied segmentation is required by the complete-record policy contract and is not segmentation discovery.

Independent saved-output validation passes all840 scientific readouts. It reconstructs arrived packet hashes, exact retention decisions and reservoir draws, destructive recent eviction, event/descriptor preservation, capacity and replacements, removal, candidate witnesses and output-availability ordering. A separate raw-response recount agrees on all560 held-out outcomes. All855 qualification/scientific starts have one completion; no replay, candidate exception or resource breach occurred.

The broker records actual receipt times and a separate simulated delivery-availability timestamp after evidence arrival, construction, matching and transfer. Accelerated simulation is not presented as real-time listening latency. Dense matcher inputs are transient scratch; retained evidence, descriptors, RNG state and accessible caches are counted. The unpressured reference has a separate512-record/8 MiB bound and is not selectable.

## Resources and provenance

855/900calls;2GiB artifact ceiling;7200aggregate-second ceiling; $0. See `resources.json` for conservative active-time/helper charges, the pre-limit preparation charge, publication reserve, peak RSS, scratch and actual processing/delivery measurements. No audio rendering/extraction, MERT operation, new model, download, training or credit reset occurred. The interrupted resource-wait period was excluded from active compute.

Actual execution revision: `1004d09460239742544766d6dbfa93c38a578097`.
Preregistration: [PR2 comment5848627639](https://github.com/trinitron88/earworm/pull/2#issuecomment-5848627639), posted before evaluation. `freeze.json` hashes2,003files at that revision. The later publication commit adds evaluation outputs and this report; it is not retroactively called the execution revision.

## Next owner and stop

The assigned comparison is complete. The worker publishes this ResultC snapshot and stops awaiting exact-head review. **PM owns the next bounded decision about the query/correspondence contract.** These findings do not authorize a repair, larger capacity, new policy, another seed, acoustic comparison or another experiment. Historical PR20/PR19/PR18, E08 and the release checklist remain preserved.

Key evidence: `score-evaluation.json`, `validation-evaluation.json`, `independent-recount.json`, `unpressured-misses.json`, `retention-losses.json`, `outputs/`, `PROTOCOL.md`, `input-manifest.json`, `material-provenance.json`, `resources.json`, `publication.json`, `preservation.json`, and `checksums.json`.
