# Correspondence choice: timing improves, unsupported identity remains unresolved

**Preparation Result A; scientific comparison Result B. No equal-observation method qualifies.** All264 musical and24 separate diagnostic readouts completed once, and saved-output validation passed. The predeclared decision selects a narrow future contract revision: when observations cannot identify which equivalent earlier occurrence supplied a return, preserve the compatible occurrence set or abstain. It does not authorize another experiment.

This is an eight-family **development comparison**, not held-out confirmation, audio-route adoption or a claim about perceived musical identity. D is the unchanged historical readout. G checks one global pitch/time transformation against every observed run. G-exact uses that same transformation model with richer exact-event timing/onset information and is a diagnostic only.

## Fixed musical panel

Each positive cell has eight cases. Every number below includes rejected cases in its denominator.

| Method | Condition | Correct source | Correct source + exact pitch shift | Correct source + scale within10% | Joint identity/shift/scale |
|---|---|---:|---:|---:|---:|
| D, sampled rows | Unchanged |8/8|8/8|5/8|5/8|
| D, sampled rows | Transposed |8/8|8/8|5/8|5/8|
| D, sampled rows | Stretched |8/8|8/8|6/8|6/8|
| G, same sampled rows | Unchanged |7/8|7/8|7/8|7/8|
| G, same sampled rows | Transposed |7/8|7/8|7/8|7/8|
| G, same sampled rows | Stretched |5/8|5/8|5/8|5/8|
| G-exact, richer events | Unchanged |8/8|8/8|8/8|8/8|
| G-exact, richer events | Transposed |8/8|8/8|8/8|8/8|
| G-exact, richer events | Stretched |8/8|8/8|8/8|8/8|

| Method | Accepted foils (limit1/16) | Accepted substitutes after source removal (limit2/24) | Accepted recent-only returns (limit2/24) |
|---|---:|---:|---:|
| D |1/16|12/24|0/24|
| G |1/16|3/24|0/24|
| G-exact diagnostic |0/16|0/24|0/24|

D retains excellent source/pitch recovery but fails every per-cell timing gate and the source-removal refusal gate. G estimates the correct pitch and time scale in all19 accepted positive cases, yet falls below the7/8 stretched-cell requirement and accepts one substitute above the source-removal limit. Neither D nor G meets the complete criteria. G-exact meets the numerical requirements but cannot qualify an equal-observation candidate.

G's five positive rejections all report **multiple full-consistent occurrence regions under its fixed sampled-query model**, not a lack of any candidate. Every accepted G estimate retains a nonzero feasible parameter range. Its single representative scoring correctly does not make the timing exact. All24 G-exact positive transformations are correct without sampled-boundary uncertainty.

Strict fragment localization, reported separately from the gates, succeeds for11/24 D positives,19/24 G positives and24/24 exact positives. G's19 localized cases are its19 accepted positives. D uses outer-bin query support and G uses center-to-center support; this is disclosed rather than silently scored as the same endpoint definition. Full per-family, stratum, conditional-estimation, coverage and uncertainty tables are in `score.json`.

## What the comparison establishes

On identical sampled observations, complete-evidence verification reduced accepted source-removal substitutes from12to3 and improved timing among accepted matches, while withholding five identities D selected. It exposes a recall/refusal tradeoff under the fixed query contract. Neither implementation is ready to serve as the qualified development reference.

The exact-event diagnostic succeeds with additional timing and onset information on the same support. That locates an observation/adapter distinction worth preserving in the next decision. It does **not** establish an acoustic encoder failure: no audio encoder was run here, and exact events are richer information. Nor does it establish that every possible readout of the sampled rows must fail. The two candidates and the fixed two-second query remain specific implementation choices.

## Separate diagnostic evidence and Result B

In all four deliberately skip-sensitive cases, D accepts a zero-cost path despite an observed contradiction; G and G-exact reject. This directly demonstrates the declared difference between charged DTW anchors and complete observed-evidence consistency.

The paired-world cases contain identical entire predictor inputs, including all history, query rows, occupancy, timestamps and completed-unit availability, but stipulate different origins among two identical earlier whole occurrences. Independent hashing and exact event-map arithmetic verify both explanations. D chooses one; G and G-exact retain two candidates and abstain. A longer query cannot resolve which of two identical whole occurrences causally supplied the return.

This **constructed provenance ambiguity**, kept outside ordinary rates, satisfies the predeclared Result B witness requirement when neither sampled method qualifies. It supports only the stated equivalent-occurrence-set/abstention revision. It does not prove that the musical panel is information-theoretically ambiguous, explain all five G rejections, or turn their restricted-readout ties into whole-input impossibility certificates. The constant-pitch diagnostic additionally shows that an occurrence can be unique under the tested representation while its scale remains weakly constrained.

## Construction correction and preserved evidence

PR19 remains byte-identical and its original failed arrays remain missing. This continuation reused all35 saved sessions byte-for-byte and constructed only the fixed fourth family's five unsaved sessions, once. The independent guard checks all12 ordered onsets and offsets under one pitch translation and one positive affine time map, with a fixed1e-9second arithmetic allowance.

In the **new** fourth-family witness, windows2and15 share the source's pitch-interval pattern; only window2 matches its full pitch/time structure. Window15 has scale4/3 but a maximum endpoint residual of about0.2963seconds. Every candidate-window index, array and residual was saved before the acceptance decision. The old pitch-only veto was therefore insufficient for this new preparation's complete-equivalence question; this is not a recovered witness of PR19's original execution.

The corrected guard admits all40 fixed sessions. No family, seed, rhythm, foil, distractor or scoring threshold was replaced or tuned. The runner's manifest schema, scorer and validator were completed before preregistration. D, G, observation adapters and engine semantics remained byte-identical to PR19.

## Integrity, resources and next disposition

Execution commit: `30e1ffaa6285dc0ba09c53aca13490aefec95b39`.
Preregistration: [PR2 comment5843729275](https://github.com/trinitron88/earworm/pull/2#issuecomment-5843729275), saved and verified before the first musical readout.

The saved-only validator passed all288 readouts: immutable input/method hashes, destructive memory controls, retained-state accounting, completed-unit availability and output-receipt timestamps, D path arithmetic, G correspondences/coverage/feasible ranges, and independent paired-world checks. Predictions were stored before outcome scoring. No started musical readout was replayed. No operating-system security or dynamic access-denial claim is made for the installed Python API guard.

Cumulative charge is **320/400 calls**, including all32/32 allowed tiny qualification calls;264musical plus24diagnostic calls were executed once. No model/audio operation, new musical acquisition or spending occurred. `resources.json` carries forward3770.938094prior conservative seconds and separately counts this invocation, parallel helpers and publication reserve against3400additional/7200cumulative seconds. Rest between invocations is excluded. New-stage artifacts and prior-stage artifacts share the512MiB cap. Persistent replacement pressure remains zero; this was a clean-observation simulation, not another demonstration of raw-audio eviction.

**Next action:** review the exact published snapshot, then let the PM choose one bounded disposition using the qualified distinctions above. The worker stops. No acoustic comparison, capacity study, threshold change, matcher repair or confirmatory run is authorized by this result.

Evidence: `score.json`, `validation.json`, `preflight.json`, `freeze.json`, `inputs/panel/construction-witnesses/`, `inputs/diagnostics/ambiguity-verification.json`, `qualification-additional.json`, `resources.json`, `preservation.json`, `publication.json`, and all raw `outputs/` receipts.
