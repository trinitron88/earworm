# Correspondence construction repair and first comparison

**Preparation A; comparison B. Neither sampled-observation method qualifies.** Read [REPORT.md](REPORT.md).

The prior35sessions are reused unchanged; the fixed missing5were constructed once. All264musical and24diagnostic readouts are saved. Exact-event success is a richer-input diagnostic, not an equal-input win. The separately constructed ambiguity certificate supports only equivalent-origin-set/abstention semantics.

`validate_outputs.py` and `score.py` inspect saved inputs and responses without candidate execution. The immutable pre-exposure package is identified by `freeze.json` and the execution SHA in `preregistration_receipt.json`. Do not rerun `construction.py`, `run.py` or candidate code during review. The32tiny qualification allowance is exhausted, and no further scientific execution is authorized.

The worker has stopped for exact-head review and a new bounded PM disposition. PR19 and all older artifacts remain unchanged.
