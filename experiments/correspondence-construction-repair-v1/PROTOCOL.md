# Bounded construction correction and first development comparison

Authoritative assignment: PR2 comment5843317936/version1, base `c09c768b711104010e71184d495d714efe45dad7`. Original scientific criteria remain those of comment5842993369. PR19 and every earlier experiment remain immutable. This package does not recover missing PR19 arrays: newly generated artifacts have new provenance.

## Preparation decision

The evaluator independently tests complete12-event equivalence under one pitch translation and one positive affine time map, checking all onsets and offsets, including their implied rests. A fixed arithmetic-roundoff allowance is used; no candidate scores determine it. Every earlier contiguous12-event window, including figure boundaries, is inspected. Pitch-only matches remain diagnostics. `CONSTRUCTION.md` specifies the independent calculation and full witness capture.

Thirty-five completed sessions are verified and copied byte-for-byte. Only the five unsaved sessions from the fixed fourth novel family may be newly constructed, once. Source, seeds, ranges, rhythms, foils, interference, transforms and unrelated guards are unchanged. Source/history/query arrays and window residuals are captured before a possible veto. No substitute family, reroll, partial-panel execution or automatic second correction is permitted.

Preparation A requires the full fixed40-session panel to satisfy the complete construction guard and all packaging/access/scoring/resource checks. It permits the first musical comparison after execution-commit preregistration. Preparation B withholds execution for a full-equivalent extra occurrence or invalid/insufficient evidence and publishes the construction witness. Software/resource failure is INVALID; neither is a matcher-performance result.

## Frozen candidates and permitted inputs

D's historical code and zero-distance acceptance threshold, G's global matcher and observation adapter, the engine and the G-exact distinction are byte-identical to PR19. Their hashes are checked before exposure. See `GLOBAL_METHOD.md` and the original protocol for the precise sampling brackets, affine parameter conventions, complete-evidence verifier, query edge treatment, ties and uncertainty rules. Neither reference is an information ceiling.

D/G receive the same one-hot absolute-pitch/silence rows, occupancy,100ms center timestamps, unit identifiers and completed-unit availability. The last-two-second query and earlier-completed-unit history rule remain unchanged. G-exact independently receives exact events on the same permitted support; its richer observations cannot enter D/G. Evaluator labels never enter candidate search.

Each method/control uses a fresh guarded child. One-second arrivals populate a four-unit recent store and at most60 retained units. Before recent-only/source-removal prediction, original records are destructively removed and derived state is recomputed. Evaluator source intervals select whole intersecting units for removal only. No candidate may bridge a removed hole. Persistent predictor state, buffers and transient scratch/RSS remain separately accounted. The unchanged Python file/network guard is API isolation; it is not claimed as an adversarial OS sandbox or a dynamically tested security boundary.

## Complete package and single-use execution

Canonical input schema is `input-manifest.json` with `sessions[]` entries `session_id` and `path`, plus input-file hashes. The runner uses that same schema for panel and diagnostics. `preflight.py` checks all40 panel entries and all8 diagnostic entries before any musical child can start. It independently verifies row/event agreement, availability, source labels, immutable method hashes, all288 planned readouts and the separately constructed paired-world witness.

Every readout receives a unique output directory. A durable `started.json` is written before the child starts. An existing started-but-incomplete readout blocks replay. Completed readouts are reused on a later authorized resumption; no prediction is repeated. Raw responses are flushed to disk before any outcome-scoring function runs. Child prediction-finished time and parent response-received time remain distinct. Parent receipt is the actual availability upper bound; a separately named simulated bound adds roundtrip time to final observation availability. No measured audio-latency or raw-audio-eviction claim follows from this clean simulation.

## Unchanged scientific requirements

Eight development families, five conditions per family. Each positive condition has full, recent-only and source-removal readouts; each foil has full history. Each D/G/G-exact route has88 calls:264 total. Eight separate diagnostic cases add24. The reused35 and newly completed5 sessions are development, not fresh confirmation or unseen-work validation. No historical held-out inputs/results enter development or reruns.

For each of unchanged/transposed/stretched cells, a candidate must achieve accepted correct occurrence identity≥7/8; accepted correct source and exact pitch offset≥7/8; accepted correct source and time scale within10% relative error≥7/8. Zero shift and unchanged tempo count. Foil acceptance must be≤1/16, source-removal substitutes≤2/24, and recent-only accepted returns≤2/24. Access, preservation and budgets must pass. Correct-source recovery after removal is an integrity failure.

Report every family's results, both strata, conditional estimation, joint accuracy, refusal and uncertainty separately; rejected matches remain in unconditional denominators. Strict localization is a separate diagnostic: estimate both mapped fragment endpoints within0.1seconds of their construction-known times, using D's outer-bin support and G's observed-center support respectively. This localization threshold is not an adoption gate. No outcome-based exclusions occur.

Original comparison A selects an adequate equal-observation diagnostic (D when both pass). B applies if neither passes and an independently checkable ambiguity witness supports a specific future query/identity-contract revision. A deliberate duplicate-origin witness only justifies returning the compatible occurrence set or abstaining on that unidentifiable origin; it does not explain musical-panel timing errors or claim a longer query resolves identical whole phrases. C withholds qualification without such a witness. Invalid execution/access/budget evidence takes precedence. G-exact cannot qualify D/G.

## Cumulative finite resources and publication

Carry forward29 calls and **3770.938094 conservative seconds**, including the prior reserve. No more than3 additional tiny qualification calls are allowed. Reserve all288 panel/diagnostic calls before exposure. Cumulative limits remain400 total calls,32 tiny qualification calls,7200 aggregate seconds,512MiB new-study artifacts,$0. This continuation additionally permits at most3400 aggregate seconds including implementation, validation and publication. No paid resource, model/audio operation, musical acquisition or expanded experiment is authorized.

Resource accounting sums active invocation intervals plus parallel helper charges; time resting between invocations is excluded. Prior charges are never reset. Saved-output arithmetic checks do not call candidates. A resource limit preserves exact started/completed work and causes a bounded checkpoint, not replay or a higher allowance.

Before exposure, commit the full execution package and publish PR2 preregistration with code/input/criteria/qualification/resource hashes and the exact commit. Publish one complete successor with rootPROJECT_STATE.md, selected branch, all negative/invalid evidence, reproducible arithmetic and preservation ledger. Stop awaiting review. PM owns the next deliberate disposition; no automatic repair, acoustic follow-up or capacity study is implied.
